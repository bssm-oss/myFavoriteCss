# Morph UI now runs in extension-only mode.
# No backend environment file is required for local usage.
# Keep this file only as a marker that local provider keys are configured
# inside the extension side panel, not through shell environment variables.
